<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ohgiraffers.section01.mybatis.Model.dao.MenuMapper">

   <resultMap id="menuResultMap" type="com.ohgiraffers.section01.mybatis.Model.dto.MenuDTO">
       <id property="menuCode" column="MENU_CODE"/>
       <result property="menuName" column="MENU_NAME"/>
       <result property="menuPrice" column="MENU_PRICE"/>
       <result property="categoryCode" column="CATEGORY_CODE"/>
       <result property="orderableStatus" column="ORDERABLE_STATUS"/>
   </resultMap>

    <select id="menuSelectAll" resultMap="menuResultMap">
        SELECT
            *
        FROM
            TBL_MENU
        WHERE
            ORDERABLE_STATUS = 'Y'
    </select>

    <select id="menuCodeSelect" parameterType="_int" resultMap="menuResultMap">
        SELECT
            *
        FROM
            TBL_MENU
        WHERE
            MENU_CODE = #{menuCode}
    </select>

    <insert id="menuInsert" parameterType="com.ohgiraffers.section01.mybatis.Model.dto.MenuDTO">
        INSERT INTO
            TBL_MENU
            (MENU_NAME, MENU_PRICE, CATEGORY_CODE, ORDERABLE_STATUS)
        VALUE
        (#{menuName},#{menuPrice},#{categoryCode},'Y')
    </insert>
</mapper>